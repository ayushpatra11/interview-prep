// Having linked list is required for questions like LRUCache.
//

struct Node {
    int val;
    Node* left;
    Node* right;

    Node(int val) : val(val), left(nullptr), right(nullptr) {}
};

class Solution {
    // implement LRU cache later...
}
